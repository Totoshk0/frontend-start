// Логические операции

// И
// const and = (x, y) {
//   return x && y;
// }
const and = (x, y) => x && y;

// ИЛИ
// const or = (x, y) {
//   return x || y;
// }
const or = (x, y) => x || y;

// НЕ
// const not = (x) {
//   return !x;
// }
const not = (x) => !x;

// ИСКЛЮЧАЮЩЕЕ ИЛИ
// const xor = (x, y) {
//   return x ^ y;
// }
const xor = (x, y) => x ^ y;
