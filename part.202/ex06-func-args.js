// const sum2 = (x, y) = x + y;
// (number, number) -> number
// number, number, number
// const sum3 = (x, y, z) = x + y + z;
// (number, number, number) -> number
// number, number, number, number
// const sum4 = (x, y, z, k) = x + y + z + k;
// (number, number, number, number) -> number
// number, number, number, number, number

// number[] -> number
let maxValue = Math.max(1, 11, 23, 2);
console.log(`maxValue: ${maxValue}`);